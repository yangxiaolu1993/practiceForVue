name: test

on:
  push:
    branches:
      - master # default branch
    paths:
      - '.github/workflows/**'
      - 'src/**'

jobs:
  copy:
    runs-on: ubuntu-latest   # 一个运行代码的虚拟机
    name: fisrt demo
    steps:
      - uses: actions/checkout@v2  # git pull 将代码拉取到 ubuntu-latest 虚拟机上
      - name: Use Node.js  # 在 ubuntu-latest 虚拟机上安装 node 环境
        uses: actions/setup-node@v1
        with:
            node-version: 14    # 安装的 node 版本为 14
      - name: Create a.txt
        run: | 
            git config --global user.email "962468124@qq.com"
            git config --global user.name "yangxiaolu1993"
            git clone https://github.com/yangxiaolu1993/imonitor-vue.git
            # cp build imonitor-vue/build-copy
            cd imonitor-vue
            ls
            git remote -v
        
